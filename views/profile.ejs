<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full bg-zinc-800 h-screen">
      <div class="p-10">
        <h3 class="text-3xl my-4 text-white">Profile </h3>
        <div class="flex w-full items-end">
          <a class="bg-red-800 text-xl text-white p-2 rounded-md " href="/logout" >LogOut</a>
        </div>
        <h2 class="text-4xl font-bold text-white  my-8"><span class="font-light">Hello</span> <%= user.name %> ✌️</h2>
        <h5 class="my-5 text-white">You Can create a new post</h5>
        <form action="/post" method="post">
          <textarea name="content" class="resize-none w-1/3 block bg-transparent text-white border-2 border-red-800 rounded-md" id="" placeholder="Write about your post ">
           
          </textarea>
          <input type="submit" value="create new post" class="bg-blue-500 cursor-pointer my-3 p-2 text-white block rounded-md">
        </form>
        <h3 class="my-3 text-lg text-zinc-400">
          Your Post
        </h3>
        <div class="post flex">
         <% user.posts.reverse().forEach((post)=>{ %>
          <div class="post w-1/5 p-3 border-2 mx-2 bg-zinc-800 text-white border-zinc-500">
            <h4 class="text-blue-500 my-2">@<%= user.username  %></h4>
            <p class="text-sm tracking-tighter"><%= post.content %></p>
            <small class="text-white my-2 block">
             <%= post.likes.length %>
             likes
            </small>
            <div class="btns flex gap-4">
               <a href="/like/<%= post._id %>" class="text-blue-500  ">
                <%= post.likes.indexOf(user._id) === -1 ? "like" :"Unlike" %>
               </a>
               <a href="/edit/<%= post._id%>" class="text-blue-500 ">Edit</a>
              </div>
         </div>
        <% }) %>
        </div>
      </div>
    </div>
</body>
</html>